# UX/UI Improvement TODO

監査日: 2026-06-13

優先度順の実行用チェックリストです。上から順に対応し、受け入れ条件をすべて満たした時点で完了とします。

進捗:

- `UX-001`から`UX-009`、`UX-011`から`UX-013`: ローカル実装・検証完了
- `UX-005`: メタデータ実装完了。Xからの実取得確認はデプロイ後に実施
- `UX-010`: Speed Insights導入完了。Productionのフィールドデータ蓄積待ち

## Status

- [ ] 未着手
- [x] 完了
- `S`: 半日以内
- `M`: 1日程度
- `L`: 2日以上

## P0: 公開前に対応

### UX-001 UI Galleryのモバイルヘッダーを修正

- [x] 320pxから390px幅でヘッダーを再設計する
- [x] 現在地を視覚的かつ意味的に識別できるようにする
- [x] UI Gallery配下でナビゲーション名を統一する

工数: `M`

対象:

- `ui-gallery/style.scss`
- `ui-gallery/buttons/style.scss`
- `ui-gallery/tooltip-behavior/style.scss`
- `ui-gallery/typography/style.scss`

受け入れ条件:

- 320px、375px、390px幅で横スクロールが発生しない
- ナビゲーション項目が欠けず、タップできる
- 現在のページを色以外でも判別できる
- キーボード操作時にフォーカスが見える

### UX-002 Infoモーダルのアクセシビリティを修正

- [x] モーダル表示中は背景を`inert`にする
- [x] `Tab`と`Shift+Tab`をモーダル内で循環させる
- [x] 開いた直後のフォーカス位置を適切に設定する
- [x] 閉じた後にInfoボタンへフォーカスを戻す
- [x] Escape、閉じるボタン、背景クリックで閉じられる状態を維持する
- [x] `aria-label`を日本語へ統一する
- [x] 見出しを`h1`から適切な階層へ修正する

工数: `M`

対象:

- `works/_shared/detail-shell.js`
- `works/_shared/detail-shell.scss`
- `works/*/index.html`

受け入れ条件:

- モーダル表示中にキーボードフォーカスが背景へ移動しない
- スクリーンリーダーがダイアログ名を読み上げる
- モーダルを閉じると起点のInfoボタンへ戻る
- ページ内の見出し階層に飛び級や重複した主見出しがない

### UX-003 タッチ・キーボード向けのデモ操作を整備

- [x] Ink Bleedの案内文を入力方式に応じて切り替える
- [x] Hover系デモにフォーカスまたはタップ操作を用意する
- [x] Easingのプレビューをタッチでも確認できるようにする
- [x] Tooltipの表示・非表示をキーボードでも操作できるようにする

工数: `M`

対象:

- `works/fluid-image/`
- `easings/`
- `ui-gallery/tooltip-behavior/`

受け入れ条件:

- タッチ端末で「カーソルを動かす」など不可能な案内が表示されない
- マウスなしでも主要なデモ内容を確認できる
- フォーカス、タップ、ホバーで一貫した結果になる

### UX-004 文字の可読性とコントラストを改善

- [x] 通常文字色のコントラストを4.5:1以上にする
- [x] UI状態や境界線など重要な非テキスト要素を3:1以上にする
- [x] 日本語本文の基本サイズと行間を見直す
- [x] 8pxから10pxの文字を用途ごとに11px以上へ引き上げる
- [x] 日本語フォントの優先順位を再検証する

工数: `M`

対象:

- `src/styles/global.scss`
- `src/styles/base.scss`
- `src/styles/site-index.scss`
- 各詳細ページのSCSS

受け入れ条件:

- 通常文字がWCAG AAの4.5:1を満たす
- 日本語本文をモバイルで拡大せず読める
- 補助情報を除き、本文に11px未満の文字がない
- 200%拡大時にも内容が欠けない

### UX-005 X共有用メタデータを整備

- [x] ページ固有のOGタイトルと説明を設定する
- [x] ページ固有のOG画像を設定する
- [x] `og:url`とcanonical URLを設定する
- [x] Twitter Cardメタデータを設定する
- [x] 通常サイトと単独デモのURLを出し分ける

工数: `M`

対象:

- `src/shared/head.html`
- `vite.config.js`
- `scripts/build-share.mjs`
- 各ページの`index.html`

受け入れ条件:

- XのCard Validator相当の確認で画像、タイトル、説明が取得できる
- 通常ページと共有用ページがそれぞれ正しいcanonicalを持つ
- ページごとに共有画像と説明が変わる

## P1: 優先度高

### UX-006 主要な操作領域を拡大

- [x] ヘッダーリンクの操作領域を拡大する
- [x] カテゴリタグと補助リンクの操作領域を拡大する
- [x] モーダル内の閉じるボタンと参照リンクを確認する
- [x] 見た目を変えずに疑似要素またはpaddingで領域を確保する

工数: `M`

依存: `UX-001`

受け入れ条件:

- 主要操作は44px四方相当の操作領域を持つ
- その他の操作もWCAG 2.2の24px基準または間隔条件を満たす
- 隣接した操作を誤タップしにくい間隔がある

### UX-007 Image Wipe Gridの偽リンクを解消

- [x] 遷移しない商品カードから`a`要素を外す
- [x] 不要なキーボードフォーカスを削除する
- [x] スクロールリビールの見た目と挙動を維持する

工数: `S`

対象:

- `works/image-wipe-grid/index.html`
- `works/image-wipe-grid/style.scss`

受け入れ条件:

- カードを押しても同じ位置へ移動するだけの挙動がない
- 本当に操作できる要素だけがTab順に含まれる
- スクロールリビールが変更前と同様に動作する

### UX-008 画像と大容量アセットを最適化

- [x] PNG画像をAVIFまたはWebPへ変換する
- [x] `srcset`と`sizes`を設定する
- [x] 画面外画像へ`loading="lazy"`を設定する
- [x] `width`と`height`または同等の領域予約を設定する
- [x] 500KBのScroll Type Reveal JSを分析して削減可否を判断する

工数: `M`

対象:

- `src/assets/images/`
- `works/image-wipe-grid/index.html`
- `works/scroll-type-reveal/`

受け入れ条件:

- Image Wipe Gridの画像転送量を現状約4.5MBから大幅に削減する
- 画像読み込み時にレイアウトシフトが発生しない
- モバイル低速回線でも先頭コンテンツを優先表示する

### UX-009 スキップリンクと文書構造を整備

- [x] 全ページに本文へのスキップリンクを追加する
- [x] `header`、`nav`、`main`、`footer`のランドマークを確認する
- [x] 見出し階層をページ単位で修正する
- [x] ランドマークが複数ある場合は識別名を付ける

工数: `S`

依存: `UX-002`

受け入れ条件:

- 最初のTab操作で本文へ移動できる
- 各ページに主となる`main`が1つだけ存在する
- 見出しだけを追って内容構造を理解できる

### UX-010 Core Web Vitalsの計測を導入

- [ ] Productionでフィールドデータを収集する
- [ ] モバイルとデスクトップを分けて確認する
- [ ] ページ種類ごとにLCP、INP、CLSを確認する
- [ ] 単独デモも通常サイトと分けて計測する

工数: `S`

依存: `UX-005`, `UX-008`

受け入れ条件:

- 75パーセンタイルでLCP 2.5秒以下
- 75パーセンタイルでINP 200ms以下
- 75パーセンタイルでCLS 0.1以下

## P2: 整合性改善

### UX-011 UI Galleryの公開状態を統一

- [x] 空のButtonsページを`Coming soon`扱いにする
- [x] `available`件数を実際の公開数と一致させる
- [x] 空ページをナビゲーションへ残すか判断する

工数: `S`

受け入れ条件:

- 一覧の状態表示とリンク先の内容が矛盾しない
- 公開件数が実際に閲覧できるページ数と一致する

### UX-012 カテゴリと名称を統一

- [x] Image Wipe Gridから不要な`Hover`分類を削除する
- [x] Easings内の`Image Wipe Grid / Hover`を修正する
- [x] `Tooltip`、`Tooltips`、`Hover Intent`の表記を統一する
- [x] 詳細ページと一覧データのタグを一致させる

工数: `S`

対象:

- `src/data/works.js`
- `src/data/easings.js`
- `ui-gallery/*/index.html`

受け入れ条件:

- 同一機能がページごとに異なる名称で表示されない
- 一覧、カテゴリ、詳細ページのタグが一致する

### UX-013 外部リンクの意味を明確化

- [x] 新しいタブで開くことをアクセシブルネームへ含める
- [x] 外部リンクの視覚表現を全詳細ページで統一する
- [x] 下線アニメーションがフォーカス時にも表示されるようにする

工数: `S`

受け入れ条件:

- スクリーンリーダー利用者が新しいタブで開くことを認識できる
- ホバーなしでも外部リンクだと判別できる

## 推奨実行順

1. `UX-001` モバイルヘッダー
2. `UX-002` Infoモーダル
3. `UX-003` タッチ・キーボード操作
4. `UX-004` 可読性・コントラスト
5. `UX-005` X共有メタデータ
6. `UX-006` 操作領域
7. `UX-007` 偽リンク
8. `UX-008` アセット最適化
9. `UX-009` 文書構造
10. `UX-010` Core Web Vitals
11. `UX-011`から`UX-013`の整合性改善

## 完了時の共通確認

- [x] `npm run build`が成功する
- [x] 320px、375px、390px、768px、1440pxで主要ページを確認する
- [x] キーボードのみで主要導線を操作できる
- [x] `prefers-reduced-motion: reduce`で不要な動きが止まる
- [x] Chrome DevToolsまたは同等ツールで重大なアクセシビリティエラーがない
- [x] 通常サイトと単独デモの両方をProduction相当で確認する
